{
  "name": "gh-label-sync-rc",
  "version": "0.0.3",
  "description": "This tool allows you to synchronize labels between two GitHub repositories. It retrieves the labels from a source repository and updates the labels in the target repository.",
  "type": "module",
  "author": "Your Name",
  "license": "AGPL-3.0-only",
  "homepage": "https://ribeiro-tiago.github.io/nuxt-lazyload-files",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ribeiro-Tiago/nuxt-lazyload-files.git"
  },
  "bugs": {
    "url": "https://github.com/owner/project/issues"
  },
  "keywords": [
    "github",
    "devtools",
    "typescript",
    "bun"
  ],
  "main": "./dist/app.js",
  "types": "./dist/types.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "packageManager": "bun@1.2.5",
  "scripts": {
    "dev": "bun run src/app.ts",
    "build": "tsc",
    "publish": "bun run build && bun changelogen --release --publish --push",
    "publish:beta": "bun run build && bun changelogen --publishTag rc --prerelease --nameSuffix rc --push"
  },
  "devDependencies": {
    "@commander-js/extra-typings": "^13.1.0",
    "@eslint/eslintrc": "^3.3.1",
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^8.28.0",
    "@typescript-eslint/parser": "^8.28.0",
    "changelogen": "^0.6.1",
    "consola": "^3.4.2",
    "eslint-config-prettier": "^10.1.1"
  },
  "peerDependencies": {
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "commander": "^13.1.0"
  }
}